# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Claude Code local config
.claude/
.vercel

# M13 audio fixture — a real recording (large + copyright); loaded via the
# file-picker at runtime, so it never needs to ship. The synthetic sample
# charts stay committed.
public/samples/Pulse_and_Shine_*
